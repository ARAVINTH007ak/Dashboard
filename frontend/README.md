# Frontend (Next.js)

## Setup
1) Copy env file:
- cp .env.example .env.local
2) Install and run:
- npm install
- npm run dev

## Env
- NEXT_PUBLIC_API_BASE points to the backend, for example http://localhost:8080
